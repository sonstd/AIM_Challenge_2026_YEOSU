@AGENTS.md

# AIM Challenge 2026 예선 — 정량평가 준수 지침

이 문서는 코드 구현 시 **반드시 지켜야 하는 제약**을 정리한 것이다.
위반 시 0점 처리되거나 평가에서 제외되는 항목이 포함되어 있으므로,
추천 API·검증 로직·데이터 처리 코드를 작성하거나 수정할 때 항상 이 문서를 기준으로 한다.

---

## 0. 배점 구조 (우선순위 판단용)

```
예선 정량점수 = 추천 근거성 평가 × 0.60 + 이미지 LoRA 일관성 평가 × 0.40
```

### 추천 근거성 평가 (예선 전체의 60%)
| 세부 항목 | 비중 | 무엇을 재는가 |
| :--- | :--- | :--- |
| 장소 유효성 | 20% | place_id가 기준 데이터에 실제 존재하는가 |
| 의미 근거성 | 50% | 추천 이유 ↔ 기준 근거(evidence_text) 5차원 의미 유사도 |
| 설명 충실도 | 30% | 문장별로 근거에 기반하는가 (환각 여부) |

평가 모델: **BGE-M3 (BAAI) 언어 임베딩**

### 의미 근거성 산식
```
근거성 = 0.70 × max(객관 점수, 주관 점수) + 0.30 × 테마 점수
```
- 객관 축 ← `evidence_text_1`
- 주관 축 ← `evidence_text_2`
- 기타 축 ← `evidence_text_3~5`
- 테마 축 ← `summary` + `category` (없으면 `embedding_text` → `evidence` 자동 대체)
- 선호 축 ← `preference_tags` / `avoid_tags` (점수에 직접 반영되지 않음)

---

## 1. 절대 위반 금지 (0점 / 평가 제외 사유)

| # | 규칙 | 위반 시 |
| :-- | :--- | :--- |
| 1-1 | `place_id`는 기준 CSV에 존재하는 값만 사용 | 유효성 + 근거성 + 충실도 **전부 0점** |
| 1-2 | `region_id`·`place_id` 오기입/미기입 금지 | 유효성 평가 **0점** |
| 1-3 | 동일 장소 중복 추천 금지 | 중복 항목 **무효 처리** |
| 1-4 | 추천 이유는 **2문장 이상** | 해당 장소 추천이유 영역 **0점** |
| 1-5 | 제출 JSON과 REST API 호출 결과가 일치해야 함 | 감점 또는 **평가 제외** |
| 1-6 | 자체 서버 운용 시 REST API 소스코드·구동방법 필수 포함 | 재현성 검증 **불통과** |

---

## 2. 문장 처리 규칙 (가장 사고가 잦은 지점)

### 2-1. 문장 구분자
```
인정: . ! ?
불인정: , (쉼표)
```
검증 코드는 **반드시 위 3개 문자로만** split 하고, 빈 문자열은 제외한 뒤 개수를 센다.
평가 시스템과 동일한 방식이어야 한다.

### 2-2. 문장 수 제한
- **2문장 미만** → 해당 장소 0점
- **5문장 초과** → 앞 5문장만 반영 (뒤는 버려짐)
- **권장: 3문장** (하한선에서 안전마진 확보)

### 2-3. 채점 방식 — "문장별 독립 판정"
추천 이유는 문장 단위로 쪼개져 **각 문장이 개별적으로** evidence 5개 축과 비교된다.
지지된 문장 비율로 충실도가 계산된다.

```
근거 있는 문장 2개 → 2/2 지지 → 충실도 100점
근거 있는 문장 1개 + 근거 없는 문장 2개 → 1/3 지지 → 충실도 33점
```

**함의: 문장 수를 늘리면 분모만 커진다. 근거 확실한 문장만 쓸 것.**

### 2-4. 문장 구성 규칙 (권장 3문장 구조)
| 순서 | 대응 축 | 내용 | 필수 조건 |
| :-- | :--- | :--- | :--- |
| 1문장 | 객관 (`evidence_text_1`) | 수치·지정·연혁 등 검증 가능한 사실 | **장소명 포함** (테마 축 유사도 상승) |
| 2문장 | 주관 (`evidence_text_2`) | 분위기·감각·정서 | — |
| 3문장 | 기타 (`evidence_text_3`) | 추천 활동·체험 | — |

### 2-5. 소수점 수치 — 절대 금지 ★최우선

`.`이 문장 구분자로 처리되므로, 소수점이 포함된 수치를 인용하면
**한 문장이 두 조각으로 쪼개진다.** 앞 조각은 의미가 잘려 근거 지지를 받지 못하므로
충실도 분모만 늘어난다.

```
입력:  "돌산공원과 자산공원 사이 1.5km 바다를 건너는 케이블카입니다."
파싱:  ① "돌산공원과 자산공원 사이 1"     ← 지지 실패
       ② "5km 바다를 건너는 케이블카입니다" ← 의미 훼손
```

**기준 데이터에 실제로 존재하는 소수점 수치 (인용 금지 대상)**

| place_id | 장소 | 해당 수치 | 등장 필드 |
| :--- | :--- | :--- | :--- |
| YEOSU_002 | 여수해상케이블카 | `1.5km` | summary, evidence_text_1 |

> 위 표는 확인된 것만 기재한 것이며, 데이터 갱신 시 재점검할 것.
> 정수 단위(`450m`, `768m`, `300년` 등)는 `.`이 없으므로 인용해도 무방하다.

**처리 방침**
- Agent 프롬프트에 "소수점이 포함된 수치는 인용하지 말 것"을 명시
- 필요하면 `약 2km`처럼 정수로 바꾸거나, 수치를 생략하고 서술
- **검증 레이어에서 소수점 발견 시 경고가 아니라 재생성 처리** (3-[5] 참조)

### 2-6. 그 밖의 텍스트 금지 사항
| 금지 | 이유 |
| :--- | :--- |
| 작은따옴표·큰따옴표 인용 | 원문 evidence에 없는 기호가 임베딩 유사도를 희석 |
| evidence에 없는 사실·과장 | 설명 충실도 감점 |

### 2-7. 원문 복사 금지 ★

evidence_text를 어미만 바꿔 그대로 옮기면 유사도 점수는 높게 나오지만,
아래 두 가지 위험이 있다.

1. **육안 검토** — 주의사항에 "운영진의 구동 점검 및 육안 검토를 병행할 수 있다"고
   명시되어 있다. 모든 추천이 원문 복사면 "Agent가 생성했다"기보다
   "필드를 이어붙였다"로 보일 수 있다.
2. **서비스 품질** — 사용자 취향을 반영한 흔적이 전혀 남지 않는다.

```
✗ 나쁜 예 (원문 복사)
  ev1 원문 : 이순신광장과 여수해양공원 사이의 해안 공원. 낭만포차 거리와
             버스킹 무대가 있는 밤바다 명소다.
  생성 문장: 종포해양공원은 이순신광장과 여수해양공원 사이의 해안 공원으로
             낭만포차 거리와 버스킹 무대가 있는 밤바다 명소입니다.
             ← 장소명만 붙이고 어미만 변경

✓ 좋은 예 (사실 유지 + 재구성)
  생성 문장: 종포해양공원은 이순신광장과 여수해양공원을 잇는 해안 공원으로,
             밤이면 낭만포차 거리와 버스킹 무대가 문을 엽니다.
```

**원칙**
- evidence의 **사실은 유지**하되, 문장 구조와 어순은 새로 구성한다
- 원문을 그대로 옮기거나 어미만 바꾸지 않는다

**category 용어 보존 (의미 근거성·테마 축 직결)**
- 각 장소의 `category` 단어가 evidence_text에도 등장하면, **대체할 수 없는 표현은 반드시 원문 그대로** 사용한다.
  - 고유 표현(시설명·고유 지명·고유 명칭): `예술섬`, `해상케이블카`, `크리스탈캐빈`, `장도전시관`,
    `예울마루`, `비렁길`, `낭만포차`, `진남관`, `돌산도`, `장군도`, `한려수도`
  - 흔한 단어라도 대체하면 의미·표기가 달라지는 명칭: `몽돌해변`, `아쿠아리움`, `벨루가`,
    `사장교`, `암자`, `버스킹`, `등대`, `백사장` 등
- 반면 **바꿔도 의미가 그대로인 일반어는 고정 대상이 아니다.** 자유롭게 바꿔 써도 된다.
  예: `공원`, `산책`, `전망`, `해변`, `섬`, `여수`, `휴식`, `관람`, `체험`
- `자연관광`·`야간관광`·`도시관광`·`역사문화`·`체험시설` 같은 **분류 라벨은 category에만 있고
  evidence에는 등장하지 않아 판정 대상이 아니다.** 일반어 목록에도 넣지 않는다.
- **판단 기준은 범용성이 아니라 대체 가능성이다.**

  | 단어 | 범용어인가 | 대체하면 | 판정 |
  | :--- | :--- | :--- | :--- |
  | `산책`, `전망` | 예 | `걷기`, `조망` — 의미 동일 | 일반어 |
  | `몽돌해변` | 예 | 대체어 없음 | **고정** |
  | `아쿠아리움` | 예 | `수족관` — category 표기와 어긋남 | **고정** |
  | `벨루가` | 예 | `흰돌고래` — 의미는 통해도 표기가 달라짐 | **고정** |
  | `사장교`, `암자` | 예 | `다리`, `절` — 상위 개념으로 뭉개짐 | **고정** |
  | `건축` | 예 | 진남관 "목조 관아 **건축**"은 양식을 뜻함. `건물`이면 구조물 하나로 좁아짐 | **고정** |

- 재구성은 문장 구조와 어순에서 한다. **고정 대상을 동의어나 상위 개념으로 대체하지 않는다.**
- **원문 복사 판정([3]-[7]) 회피는 고정 대상이 아닌 말을 의미가 달라지지 않게 유지하는 선에서
  최대한 다양하게 바꾸는 방식으로 한다.**
  일반 명사·서술어·연결 표현·문장 구조·절 순서를 적극적으로 바꾸고, 고정 대상은 그 사이에 그대로 둔다.

  | 구분 | 원문 → 생성 | 판정 |
  | :--- | :--- | :--- |
  | 뜻이 같은 교체 | 한 번에 담는 → 한눈에 모으는 | ✓ 허용 |
  | 뜻이 같은 교체 | 예술 섬으로 조성한 → 예술 섬으로 꾸민 | ✓ 허용 |
  | 뜻이 같은 교체 | 바다를 건너는 → 바다 위를 가로지르는 | ✓ 허용 |
  | 뜻이 바뀐 교체 | 밤을 대표하는 활기 → 밤 특유의 활기 | ✗ 금지 |
  | 뜻이 바뀐 교체 | 전라좌수영 객사로 국보 → 객사 역할을 했던 국보 | ✗ 금지 |

  고정 단어가 늘수록 원문과 글자가 겹칠 수밖에 없으므로, 나머지 부분의 변형 폭으로 이를 상쇄한다.

- **어구 순서는 evidence와 다르게 배치한다.** 원문이 A-B-C 순서라면 B-A-C처럼 앞뒤를 바꾸고,
  자연스럽게 대체할 서술 표현이 있으면 다른 표현을 쓴다.

  ```
  원문 : 물결을 형상화한 곡면 외벽이 바다와 맞닿아 해양 도시의 랜드마크다운 인상을 준다.
  ✓ 좋음: 곡면 외벽에 물결이 형상화되어 있고 바다와 이어져 있어 해양 도시의 랜드마크다운 인상을 풍긴다.
         (어구 순서 변경 + 맞닿아→이어져, 준다→풍긴다)
  ```

  ```
  원문 : 언덕 위에서 오동도와 한려수도가 내려다보여 여수 바다의 첫인상을 시원하게 열어 준다.
  ✗ 나쁨: 언덕 위에서 오동도와 한려수도가 내려다보여 여수 바다의 첫인상을 시원하게 열어 줍니다.  (86%)
  ✓ 좋음: 오동도와 한려수도를 발아래 두는 언덕이라 여수 바다가 첫인상부터 시원하게 트입니다.
  ```

  | 장소 | evidence 원문 | 판정 |
  | :--- | :--- | :--- |
  | 장도 | 예술 섬으로 조성한 → 문화 예술 공간으로 가꾼 | ✗ 고유 표현 `예술 섬` 소실 |
  | 장도 | 예술 섬으로 조성한 → 예술 섬으로 꾸민 | ✓ `조성한`은 고유 표현이 아님 |
  | 이순신광장 | 거북선 실물 모형 → 실물 크기의 거북선 모형 | ✗ 의미 변형 |
  | 여수해상케이블카 | 해상 케이블카 → 해상 탑승 시설 | ✗ 고유 표현 소실 |

- **evidence_text에 없는 말을 덧붙이지 않는다 (엄격 — 한 단어도 허용하지 않음)**
  바꿔 쓰기는 evidence에 이미 있는 말을 **교체·재배열**하는 것이다. 원문에 대응하는 말이 없는
  형용사·부사·꾸밈 구절을 넣는 것은 **덧붙이기**다. 어조는 어미로만 조정한다.
  실제로 새어 나왔던 예:
  `낭만포차의 맛있는 먹거리`, `높이 위치한 언덕`, `다 함께 즐길 수 있습니다`,
  `높은 언덕`, `아름다운 조명`, `멋진 야경`, `남다른 스케일`, `온전히 느끼게`
  Agent에는 "문장을 다 쓴 뒤 형용사·부사·꾸밈 구절을 하나씩 짚어 evidence에 같은 뜻의 말이
  있는지 확인하고, 없으면 지운다"는 자기 점검 절차를 지시한다.
  한국어 꾸밈말 추가는 형태소 분석 없이는 코드로 정확히 잡기 어려워, 현재는 프롬프트로 막고
  제출 전 육안으로 확인한다.
- **3문장의 활동 나열은 최대 3개**까지만 쓴다
  (evidence_text_3에 4개 이상 나열된 경우 대표적인 것만 고른다. 현재 데이터에서는 오동도가 4개)
- **evidence_text_3의 쉼표 나열 방식을 따르지 않는다.** 활동을 쉼표로 늘어놓지 말고
  `-거나`, `-고`, `-며` 같은 연결어미로 이어 한 문장으로 만든다. 활동 순서도 원문과 다르게 두고,
  활동 명사는 자연스러운 서술형으로 풀어 쓴다(고정 대상 표기는 유지).

  ```
  원문 : 아쿠아리움 관람, 건물 외관 촬영, 박람회장 단지 산책에 좋다.
  ✗ 나쁨: 아쿠아리움 관람, 건물 외관 촬영, 박람회장 단지 산책을 즐기기 좋습니다.  (쉼표 나열 + 원문 순서)
  ✓ 좋음: 건물 외관의 사진을 찍거나 아쿠아리움을 관람하거나 박람회장 단지에서 산책하기 좋다.
  ```

**구현**
- 일반어 목록은 `src/config/category-terms.js` 에 **짧게** 명시한다. 목록에 없는 단어는 모두 고정한다
  (애매하면 잃는 쪽보다 지키는 쪽이 안전). **빈도로 자동 판별하지 않는다** —
  `돌산도`는 3곳, `낭만포차`는 2곳의 category에 있지만 둘 다 고정 대상이다.
- 코드가 category ∩ evidence_text_1~5 중 고유 표현을 **공백을 무시하고 대조해 evidence 원문 표기로**
  뽑아 `keep_terms`로 Agent에 전달한다 (`예술섬` ↔ `예술 섬`). `category` 필드도 함께 넘긴다.
- places.json이 바뀌면 category ∩ evidence 용어를 다시 뽑아 일반어 목록을 점검한다.
- 사용자의 **여행 유형(16종)과 여행 조건**(동반·기간·경비·이동수단)을 **어조에 반영**한다
  (단, evidence에 없는 사실을 추가하지 않는다. 조건 자체를 문장에 언급하지도 않는다)

---

## 3. 서버 검증 레이어 필수 항목

Agent 응답을 받은 직후, 사용자에게 반환하기 **전에** 아래를 기계적으로 검증한다.
실패 시 재호출(최대 3회), 그래도 미달이면 에러 반환.

```
[1] place_id가 전체 20곳 화이트리스트에 존재하는가        → 실패 시 재호출
[2] place_id 중복이 없는가                              → 중복 제거
[3] 추천 장소 수가 정확히 3개인가                        → 실패 시 재호출
[4] 각 추천 이유의 문장 수가 2~5개인가 (.!? 기준)        → 실패 시 재호출
[5] 소수점 포함 수치가 있는가  ★재생성★                  → 실패 시 재호출
[6] 인용부호(' ")가 포함되어 있는가                      → 제거 후 통과
[7] evidence_text 원문과 지나치게 동일하지 않은가         → 실패 시 재호출 (거부 문장을 프롬프트에 실음)
```

**[5] 소수점 검출 정규식**
```
/\d+\.\d+/
```
숫자-마침표-숫자 패턴이 하나라도 있으면 즉시 재생성한다.
경고만 남기고 통과시키면 안 된다 (2-5 참조).

**[7] 원문 복사 검출 → 재호출**
생성 문장과 대응 evidence_text(1문장↔1, 2문장↔2, 3문장↔3)의 문자 단위 일치율이
**80% 이상**이면 재호출한다. 다음 호출 프롬프트에 거부된 문장을 아래 형식으로 싣는다.

```
## 원문 복사로 거부된 문장
이전 시도가 아래 문장에서 원문 복사로 거부되었다. 다시 작성하라.

[YEOSU_013 · 2문장]
  원문 : 포차 불빛과 바다, 음악이 뒤섞인 저녁 풍경이 여수 여행의 밤을 대표하는 활기를 준다.
  거부 : 포차 불빛과 바다, 음악이 뒤섞인 저녁 풍경이 여수 여행의 밤을 대표하는 활기를 선사합니다.
  문제 : evidence_text2와 동일한 어휘, 표현이 동일한 어순으로 사용되었다.
        keep_terms(낭만포차, 버스킹, 해안 데크)을 제외하고, 어휘, 표현, 어순 중 적어도 한 가지는 반드시 바꿔서 다시 작성하라.
```

- `keep_terms`에는 그 장소의 고정 표현 전체를 보여 준다.
- 원문 복사로 걸린 항목은 **결과에서 빼지 않는다.** 장소 자체는 유효하므로, 3회를 다 써도
  복사가 남으면 항목 수가 같은 시도 중 **복사 거부가 가장 적은 시도**로 응답한다(500으로 끝내지 않음).
- Agent에 닿지 못했을 때의 대체 응답(evidence 조립)에는 적용하지 않는다. 구조상 항상 원문과 같기 때문이다.

**중요**: LLM 출력은 매번 흔들리므로, 위 검증을 프롬프트 지시에만 의존하지 말고
반드시 코드로 강제할 것.

---

## 4. 역할 분리 규칙 (LLM vs 코드)

| 항목 | 담당 | 이유 |
| :--- | :--- | :--- |
| 장소 선정 (`place_id`) | LLM | 취향 해석이 필요 |
| 추천 이유 (`recommend_reason`) | LLM | 자연어 생성 |
| 성향 점수 `X`/`Y` | **코드** | 8문항 응답의 단순 합 = 결정적 연산. 클라이언트 값을 믿지 않고 서버가 재계산 (5-3) |
| 여행 유형 (`typeId`) | **코드** | X/Y 구간 판정 = 결정적 연산 (5-3) |
| 매칭도 | **코드** | 좌표 거리 계산 (5-4). LLM이 숫자를 만들면 근거 없는 값이 된다 |
| `matched_tags` | **코드** | 축 방향 태그 ∩ preference_tags + 동반 자동 태그 = 결정적 연산 (5-5) |
| `image_prompt` | **코드** | places.json의 고정값을 조회. 캐싱 이미지와 반드시 일치해야 함 |
| `place_name` | **코드** | place_id로 조회 |
| `region_id` | **코드** | 상수 `"YEOSU"` |

**`image_prompt`를 LLM이 매번 생성하면 안 되는 이유**:
캐싱된 이미지는 고정 프롬프트로 생성된 것이므로, 프롬프트가 요청마다 달라지면
이미지-프롬프트 불일치가 발생해 프롬프트 정합성 평가(CLIP Score)에서 불리해진다.

---

## 5. 사용자 입력 규격 (프론트엔드)

```
시작 → 여행 조건 4문항 → 여행 성향 8문항 → 성향 결과 → 맞춤 추천 BEST 3
```

### 5-1. 여행 조건 4문항 (각 단일 선택, 필수)
```
Q1 누구와 함께 여행하나요?      혼자 / 친구 / 연인 / 가족
Q2 여수에서 얼마나 여행하나요?   반나절 / 당일치기 / 1박 2일 / 2박 3일 이상
Q3 여행 경비는?                최대한 알뜰하게 / 적당히 쓰면서 즐긴다 /
                              특별한 경험이라면 비용을 더 써도 된다
Q4 여수에서는 어떻게 이동?       도보 + 대중교통 / 자가용 / 렌터카 / 아직 정하지 않았다
```
- **조건 문항은 X/Y 점수에 절대 반영하지 않는다.** 후보 감점과 Agent 어조 조정에만 쓴다.
- 경비(Q3)는 places.json에 가격 데이터가 없어 **순위에 영향을 주지 않는다.**
  현재는 Agent 어조 전달용이다. 순위에 넣으려면 장소별 가격대 데이터가 먼저 필요하다.

### 5-2. 여행 성향 8문항 (필수)
노출 순서: `X1 → Y1 → X2 → Y2 → X3 → Y3 → X4 → Y4`
각 문항은 A/B 대표 카드 + 4단계 응답으로 구성한다.

| 응답 | 값 |
| :--- | :--- |
| A가 훨씬 좋다 | −2.5 |
| A가 조금 더 좋다 | −1.25 |
| B가 조금 더 좋다 | +1.25 |
| B가 훨씬 좋다 | +2.5 |

- **값(숫자)은 화면에 절대 노출하지 않는다.** 라벨만 보여준다.
- 8문항을 모두 답해야 결과 화면으로 넘어간다. 서버에서도 다시 검사한다.
- `아이와함께`·`로맨틱·커플`은 사용자 선택 항목이 아니다.
  동반 유형에 따라 스코어링에서 자동 가산한다 (5-4 참조).

### 5-3. 점수 계산 (반드시 코드 함수로 구현)
```
X = X1 + X2 + X3 + X4     (−10 ≤ X ≤ 10)   자연·힐링 ↔ 액티비티·체험
Y = Y1 + Y2 + Y3 + Y4     (−10 ≤ Y ≤ 10)   한적·로컬 ↔ 핫플·데이트
```
구간(각 구간은 왼쪽 경계 포함): `−10~−5` → 1, `−5~0` → 2, `0~5` → 3, `5~10` → 4
`typeId` = `X{1-4}Y{1-4}` → 4×4 = 16유형

성향 강도: `<2.5` 중립에 가까움 / `<5` 약한 선호 / `<7.5` 뚜렷한 선호 / `≤10` 매우 강한 선호

**X/Y는 클라이언트가 보낸 값을 믿지 않고 서버가 8문항 응답에서 다시 계산한다.**

16유형 이름은 `src/config/travel-types.js` 에 분리한다 (현재 TEMP).

### 5-4. 스코어링
```
매칭도    = 100 − (사용자 좌표 ↔ 장소 좌표 거리 ÷ √800) × 100     (0~100)
순위 점수 = 매칭도 + 동반 유형 자동 가산 − 조건 기반 avoid 감점
```
- 장소 좌표는 `src/data/place-axis.json` 에서 관리한다.
  **`places.json` 원본은 수정하지 않는다.**
- 동반 유형 자동 가산: `가족` + `아이와함께`, `연인` + `로맨틱·커플` → 각 +4
- 조건 감점: 동반 유형 / 기간 / 이동수단 감점표를 합산한다.
  하드 필터가 아니라 소프트 감점이다.
- **매칭도에는 조건 감점을 섞지 않는다.** 화면에 보이는 값은 성향 거리만 나타내야 한다.
- 최종 응답은 매칭도 내림차순으로 정렬한다
  (카드에 순위 배지와 매칭도가 함께 붙으므로 둘의 순서가 어긋나면 버그처럼 보인다).

### 5-5. matched_tags 산출 (코드가 결정)
사용자가 기운 쪽 축 태그 ∩ 장소 `preference_tags` + 동반 유형 자동 태그.

| 축 | 음수 쪽 | 양수 쪽 |
| :--- | :--- | :--- |
| X | `자연·힐링` | `액티비티` |
| Y | `문화·역사` | `야경`, `맛집·미식`, `사진·인생샷` |

- **중립 보정**: `|점수| < 2.5` 인 축은 양쪽 태그를 모두 인정한다.
  응답값이 ±1.25 / ±2.5 네 개의 합이라 축 합계가 0 부근에 몰린다. 한 축이 구간 경계
  (−5, 0, +5)에 정확히 떨어질 확률이 **27.3%**, 적어도 한 축이 경계일 확률이 **47.2%**다.
  보정이 없으면 합계가 정확히 0인 사용자에게 "0은 음수가 아니다"는 이유만으로
  반대쪽 태그가 통째로 잘려 나간다.
- 경계에 걸린 경우 결과 화면에서 유형 이름보다 "중간에 가까움"을 먼저 보여 준다.

---

## 6. 응답 포맷 (제출 JSON과 동일해야 함)

```json
{
  "region_id": "YEOSU",
  "recommendations": [
    {
      "place_id": "YEOSU_001",
      "place_name": "오동도",
      "recommend_reason": "3문장으로 구성된 추천 이유.",
      "matched_tags": ["자연·힐링"],
      "image_prompt": "places.json의 고정 프롬프트",
      "images": ["/images/places/YEOSU_001_01.png"]
    }
  ]
}
```

API 응답을 그대로 파일로 저장할 수 있어야 한다 (제출물 02번).
**손으로 편집한 JSON을 제출하지 않는다** — 규칙 1-5 위반 위험.

**본문에는 위 필드 외에 아무것도 넣지 않는다.** 화면 표시용 값(매칭도 등)은
응답 헤더로 내려보낸다. 헤더는 저장되는 JSON에 섞이지 않으므로
`curl -o` 결과가 그대로 제출물이 된다.

```
X-Match-Scores:     {"YEOSU_001":92,"YEOSU_005":90}   화면 표시용 성향 매칭도
X-Recommend-Source: agent | fallback                   생성 경로
```

---

## 7. 이미지 관련 규칙 (이미지 생성 담당자용)

### 7-1. 이미지 LoRA 일관성 평가 (예선 40%)
| 세부 항목 | 비중 | 무엇을 재는가 |
| :--- | :--- | :--- |
| 기준 유사도 | 40% | 생성 이미지가 **원본 관광지 사진**의 시각적 특성을 유지하는가 |
| 이미지 일관성 | 30% | 생성된 5장 간 스타일·색상 일관성 |
| 프롬프트 정합 | 15% | 프롬프트 텍스트 ↔ 생성 이미지 의미 일치도 (CLIP Score) |
| 색상·톤 일관성 | 10% | — |
| LPIPS | 5% | — |

평가 모델: **DINOv2-base, SigLIP2-base, LPIPS**
(팀에서도 동일 모델로 사전 검증 가능)

### 7-2. 준수 사항
- `04_대표이미지`는 **AI 생성물이 아닌 원본 사진**이어야 함
- `04`와 `05`가 동일 파일이거나 거의 동일한 복제본이면 **부정 제출 처리**
- `05_평가이미지`는 **정확히 5장** (부족 시 수량만큼 불이익)
- 5장은 **동일 프롬프트 + 시드만 변경**하여 생성 (일관성 30% 확보)
- 5장 전체에서 프롬프트·체크포인트·LoRA 강도·CFG·Steps 고정
- 여행자 캐릭터 등장은 **예선에서 불필요** (주최측 확인 완료, 가산 요소 아님)
- 원본 후보 선정 시 **간판·현판 등 텍스트가 없는 사진을 우선**한다
  (텍스트 렌더링이 깨져 유사도·일관성을 함께 해침)

### 7-3. workflow 관련
- `06_workflow.json`이 실제 생성 이미지와 무관하면 **평가 대상 제외**
- `07_workflow_assets.zip`에 모델·커스텀 노드·설정 누락 시 감점
- 오픈소스 모델의 다운로드 정보가 실제와 다르거나 모델명 임의 변경 시 **평가 제외**
- 자체 학습 모델 기재했으나 결과 이미지에 영향이 없다고 판단되면 감점
- AI Factory 자원에 제출한 workflow.json이 **반드시 존재**해야 함
- workflow의 positive prompt / text encoder 입력이 제출 이미지와 무관하면
  프롬프트 정합성 평가에서 불이익

---

## 8. 제출 전 최종 점검 체크리스트

```
[ ] 모든 place_id가 기준 CSV에 존재하는가
[ ] place_id 중복이 없는가
[ ] 모든 추천 이유가 2~5문장인가 (.!? 기준으로 실제 파싱해서 확인)
[ ] 추천 이유에 소수점 수치가 없는가  ★
[ ] 추천 이유에 인용부호가 없는가
[ ] 추천 이유가 evidence 원문의 단순 복사가 아닌가  ★
[ ] 제출 JSON이 REST API 실제 출력과 동일한가
[ ] 평가 이미지가 정확히 5장인가
[ ] 04(원본)와 05(생성물)가 명확히 다른가
[ ] workflow.json이 실제 생성에 사용된 것인가
[ ] workflow_assets.zip에 모델·노드·설정이 모두 포함되었는가
[ ] REST API 소스코드와 구동 방법이 포함되었는가
```

---

## 9. 참고: 제출 파일 구조

```
팀명.zip
├── 01_팀명_지역.csv                    (태그 보완한 기준 데이터)
├── 02_팀명_지역추천지.json              (API 출력 그대로)
├── 03_팀명_AI_Factory_인터페이스_경로_및_구동방법_가이드.txt
├── 04_팀명_대표이미지.png               (원본 사진 1장)
├── 05_팀명_평가이미지_01~05.png         (AI 생성 5장)
├── 06_팀명_workflow.json
├── 07_팀명_workflow_assets.zip
└── 08_팀명_source_code.zip             (자체 서버 운용 시 필수)
```