/**
 * category 용어 중 "일반어" 목록. (CLAUDE.md §2-7)
 *
 * category 단어가 evidence_text에도 등장하면 원문 그대로 써야 한다.
 * 단, 여기 적힌 일반어는 고정하지 않고 자유롭게 바꿔 써도 된다.
 *
 * ── 판단 기준: 범용성이 아니라 "대체 가능성" ────────────────────────
 * 다른 말로 바꿔도 의미가 그대로이고 대체어가 자연스러우면 일반어,
 * 대체어가 없거나 바꾸면 의미·category 표기와 어긋나면 고정한다.
 *
 *   일반어  공원·산책·전망    → 걷기·조망처럼 바꿔도 의미가 같다
 *   고정    몽돌해변          → 대체어가 없다
 *   고정    아쿠아리움        → 수족관으로 바꾸면 category 표기와 어긋난다
 *   고정    벨루가            → 흰돌고래로 바꾸면 의미는 통해도 표기가 달라진다
 *   고정    사장교·암자       → 다리·절로 바꾸면 상위 개념으로 뭉개진다
 *   고정    버스킹            → 거리 공연으로 바뀐 사례가 실제로 있었다
 *   고정    건축              → 진남관 "목조 관아 건축"은 양식을 뜻한다. 건물로 바꾸면 구조물 하나로 좁아진다
 *
 * 자연관광·야간관광·도시관광·역사문화 같은 분류 라벨은 category에만 있고 evidence에는
 * 등장하지 않아 애초에 판정 대상이 아니다. 목록의 의미를 흐리지 않도록 넣지 않는다.
 *
 * 흔한 단어라도 대체하면 의미가 달라지면 고정이다. 그래서 이 목록은 짧게 유지하고,
 * 목록에 없는 단어는 모두 고정 대상으로 보호한다(애매하면 잃는 쪽보다 지키는 쪽이 안전).
 * 빈도로 자동 판별하지 않는다 — 돌산도(3곳)·낭만포차(2곳)도 고정 대상이다.
 *
 * 표기는 띄어쓰기를 뺀 형태로 적는다(category 표기와 같게).
 * places.json이 바뀌면 category ∩ evidence 용어를 다시 뽑아 이 목록을 점검할 것.
 */
export const GENERIC_CATEGORY_TERMS = new Set([
  // 장소 유형·관점 — 바꿔도 의미가 같다
  "공원", "전망", "해변", "섬", "여수",

  // 행위·활동 — 걷기·쉼·구경·경험 등으로 바꿔도 의미가 같다
  "산책", "휴식", "관람", "체험",
]);
